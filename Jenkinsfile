pipeline {
    agent any

    stages {
        stage('Increment version') {
            steps {
                echo "hello world"
                sh 'pwd'
                sh 'ls -l'
                // cd node-project/app
                // npm version patch
                // cd .. && git add .
                // git commit -m "Jenkins: Increment version - patch"
                // git push
            }
        }
    }
}